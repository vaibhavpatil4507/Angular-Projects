import { PercantagePipe } from './percantage.pipe';

describe('PercantagePipe', () => {
  it('create an instance', () => {
    const pipe = new PercantagePipe();
    expect(pipe).toBeTruthy();
  });
});
